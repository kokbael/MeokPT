specVersion: "v1"
serviceId: "meokpt"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "meokpt-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
