import SwiftUI

struct DailyNutritionInfoEmptyView: View {
    var body: some View {
            Text("신체 정보가 없습니다")
                .font(.headline)
                .foregroundStyle(Color.secondary)
                .padding()
          
    }
}
